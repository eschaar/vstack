# Release 2026-05-10 — v3.1.1

## Summary

Single patch release fixing a CI publish smoke test failure introduced in v3.1.0.
No functional changes to the vstack artifact system or CLI.

## Fix

- **CI**: install wheel dependencies in the publish smoke test step so `pip install --no-deps` resolves correctly without a dependency resolution failure.

## Sign-offs

_Retrospective record — compiled from `CHANGELOG.md` and repository history. Formal
per-role sign-off was not recorded at release time._
